export enum QueryOperators {
    EQUAL = '==',
    LESS_THAN = '<',
    LESS_EQUAL_THAN = '<=',
    MORE_THAN = '>',
    MORE_EQUAL_THAN = '>=',
    DIFFERENT_THAN = '!=',
    ARRAY_CONTAINS = 'array-contains',
    ARRAY_CONTAINS_ANY = 'array-contains-any',
    IN = 'in',
    NOT_IN = 'not-in',
}

